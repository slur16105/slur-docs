declare module 'virtual:starlight/user-images' {
  // Minimal declarations so the TS language server can resolve the virtual module in .astro files
  export const logos: any;
  export default logos;
}
